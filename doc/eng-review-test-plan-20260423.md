# Test Plan
Generated by /plan-eng-review on 2026-04-23
Branch: unknown
Repo: D:\AI_project\2604_AI_news(anti)_1.5

## Affected Functions
- `deduplicate_news()` — URL dedup + new Jaccard title dedup
- `filter_news_by_ai()` — new Stage 1 ICT keyword filter + Stage 2 clustering
- `is_similar_news()` — Jaccard comparison with dual threshold
- `extract_signature()` — entity extraction for clustering

## Key Interactions to Verify
- ICT keyword filter passes ICT articles, blocks non-ICT
- ICT fallback: when < ict_min_articles pass filter, ALL articles are used (log_warning fires)
- Jaccard dedup: numeric-shared titles use threshold_numeric (0.5), others use threshold_text (0.6)
- Clustering: two articles with same signature → only representative (longest title) survives
- main_app.py imports still work after refactor (filter_news_by_ai, deduplicate_news signatures unchanged)

## Edge Cases
- `ict_keywords` missing from config → falls back to DEFAULT_ICT_KEYWORDS hardcoded list
- Zero ICT articles after filter → fallback to all articles
- All articles have same signature → one representative per cluster
- Article title with no keywords (empty string) → does not crash
- Jaccard with empty keyword sets → returns False (no crash)

## Critical Paths
- main_app.py pipeline: collect → deduplicate_news → filter_news_by_ai → analyze → report
- Function signature compatibility: both functions must accept same args as before

## Test File Location
- `tests/test_news_engine.py` (to be created)
- Framework: pytest
- No AI API calls in tests (mock all AI clients)
